#ifndef ARRAY_UTILS_H
#define ARRAY_UTILS_H

// Trova il valore minimo in un array di interi
int find_min(const int arr[], int size);

// Trova il valore massimo in un array di interi
int find_max(const int arr[], int size);

// Calcola la media dei valori in un array di interi
double find_avg(const int arr[], int size);

#endif
